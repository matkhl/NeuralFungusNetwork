{% extends 'base.html.twig' %}

{% block body %}
<div id="simulationWrapper" {{ stimulus_controller('simulation', { subscriptionUrl: mercure('networkdata')|json_encode(constant('JSON_UNESCAPED_SLASHES') b-or constant('JSON_HEX_TAG'))|raw }) }}>
    <svg></svg>
</div>
{% endblock %}